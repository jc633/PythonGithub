 		//更改顶部css
	    $('#head_bar1').attr('id', 'head_bar');
	    //跳转到登录页面
	    var to_login = function(){
    	window.location.href='/user/sign/act=login';
    	}
 		 //如果有提示
	    var msg = $('#tip-message');
	    function showMsg(con){
	    	 if(con!=''){msg.html(con);msg.show();setTimeout(function(){msg.hide(200);},5000)}
	    }
	     //关闭当前页面
    	function close_current_page(time){setTimeout(function(){window.opener=null;window.open('','_self');window.close();},time);}
